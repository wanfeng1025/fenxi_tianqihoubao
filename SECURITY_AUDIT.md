# 发布前安全审计

## 审计范围

已检查原始项目中的 Python 配置、DataX JSON、SQL、Shell、报告证据、日志和可视化资源，重点关注数据库密码、内网地址、Token、私钥、JDBC 凭据和个人本机路径。

## 原始项目发现

原始配置和部分 DataX 作业包含课程实验环境使用的数据库凭据或内网连接信息，因此没有原样上传。运行日志、截图和报告文件也可能包含环境细节或个人信息，均未纳入发布版。

## 发布版处理

- `src/config.py` 只从环境变量读取数据库连接信息，默认值为空或 `localhost`。
- 所有 DataX JSON 的用户名、密码、MySQL 主机和 Hadoop 主机均改为 `YOUR_*` 占位符。
- `.env`、密钥、日志、数据库导出和私有报告已加入 `.gitignore`。
- 发布版最终扫描目标是“真实凭据和真实环境值”，占位符不视为秘密。

## 结论

发布版未发现需要上传的真实密码、Token、私钥、内网地址或个人本机路径；使用者必须在本地通过环境变量或手工替换占位符后运行。
